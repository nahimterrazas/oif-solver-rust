I want the same API endpoints.

I want to keep the Event-driven processing 

dont implement the retry logic.

be as simple as posible since is a POC.

The configuration should be simple as posible also